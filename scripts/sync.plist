<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Nightly backup to the personal Google Drive mount.

  ProgramArguments points at ~/bin/dotfiles-backup-runner, not sync.sh. The
  runner is a compiled binary built by install.macos.yaml that execs
  scripts/sync.sh. It exists only to own the TCC identity: launchd running a
  #!/bin/zsh script makes /bin/zsh the responsible process, and macOS never
  grants an Apple platform binary access to ~/Library/CloudStorage.
  
  Install:
    cp scripts/sync.plist ~/Library/LaunchAgents/com.user.sync.plist
    launchctl load ~/Library/LaunchAgents/com.user.sync.plist
  
  Uninstall:
    launchctl unload ~/Library/LaunchAgents/com.user.sync.plist
    rm ~/Library/LaunchAgents/com.user.sync.plist
  
  Start immediately:
    launchctl start com.user.sync
  
  Check status:
    launchctl list | grep sync
-->
<plist version="1.0">
  <dict>
    <key>Label</key>
    <string>com.user.sync</string>

    <key>ProgramArguments</key>
    <array>
      <string>/Users/jimweller/bin/dotfiles-backup-runner</string>
    </array>

    <key>EnvironmentVariables</key>
    <dict>
      <key>PATH</key>
      <string>/Users/jimweller/.go/bin:/Users/jimweller/.config/dotfiles/scripts:/Users/jimweller/.local/bin:/Users/jimweller/bin:/nix/var/nix/profiles/default/bin:/opt/homebrew/opt/curl/bin:/opt/homebrew/bin:/opt/homebrew/sbin:/Users/jimweller/go/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
      <key>HOME</key>
      <string>/Users/jimweller</string>
      <key>GOPATH</key>
      <string>/Users/jimweller/.go</string>
    </dict>

    <key>RunAtLoad</key>
    <true />

    <!-- Runs daily at 2:00 AM -->
    <key>StartCalendarInterval</key>
    <dict>
      <key>Hour</key>
      <integer>2</integer>
      <key>Minute</key>
      <integer>0</integer>
    </dict>

    <key>StandardOutPath</key>
    <string>/Users/jimweller/.logs/backup-log.txt</string>

    <key>StandardErrorPath</key>
    <string>/Users/jimweller/.logs/backup-err.txt</string>
  </dict>
</plist>