# Fix Markdown

Fix markdown files using the markdown-lint skill.

Arguments: $ARGUMENTS

If a file path is provided, fix only that file. If no arguments are provided, fix all markdown files in the repository.
