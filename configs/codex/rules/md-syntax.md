# Markdown Files

Apply when writing or editing markdown files.

Load the md-syntax skill for authoring guidelines.

When only reading markdown files, no action is needed.

Skip agent artifacts entirely. `.llmtmp/` scratch, `.llmdocs/` reference docs, and
`SKILL.md` files are read by agents, not by humans, so authoring guidelines do not apply
to them.
